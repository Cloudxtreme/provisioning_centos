define host{
        use                     generic-host
        host_name               <%= @server.name %>
        alias                  	<%= @server.name %>
	address                 <%= @server[:rackspace][:private_ip] %>
        }
