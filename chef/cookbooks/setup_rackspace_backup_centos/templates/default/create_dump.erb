#! /bin/bash

DUMP_PATH="<%= @dump_folder %>/<%= @dump_file %>"
TARGET_PATH="<%= @backup_folder %>/<%= @dump_file %>"

pg_dumpall -c -U postgres -h localhost -f $DUMP_PATH

mv $DUMP_PATH $TARGET_PATH
